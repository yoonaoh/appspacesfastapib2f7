name: Trigger auto deployment for container-app-uwrevbvx3mybk

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-uwrevbvx3mybk-AutoDeployTrigger-f6561bdb-8293-4cf0-bc58-83824c853cdf.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPUWREVBVX3MYBK_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPUWREVBVX3MYBK_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPUWREVBVX3MYBK_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPUWREVBVX3MYBK_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPUWREVBVX3MYBK_REGISTRY_PASSWORD }}
          containerAppName: container-app-uwrevbvx3mybk
          resourceGroup: todonodejspgsqlgoogleautha6008334
          imageToBuild: default/container-app-uwrevbvx3mybk:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


